# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# uv / virtualenv
.venv/

# Build artifacts
build/
dist/
*.egg-info/

# Local data
data/
logs/
# ...but bundled masters snapshot IS tracked (ships with the package)
!src/uma_it_optimizer/enrich/data/

# Upstream read-only mirrors
references/

# Local fixture / screenshot folders
tests/fixtures/*.png
IT-references/

# Local project guide / editor scratch — not for the public repo
CLAUDE.md
AGENTS.md
.claude/

# Extractor API config carries a bearer token; keep it out of git.
# The .example.json version stays tracked for onboarding.
tools/memory_extractor/uma-it-config.json
