[package]
name = "edge-sdk"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "Minimal Hachimi-Edge plugin API bindings, vendored from honse-tracker."

[lib]
name = "edge_sdk"
path = "src/lib.rs"

[dependencies]
log = "0.4"
once_cell = "1.19"
parking_lot = "0.12"
